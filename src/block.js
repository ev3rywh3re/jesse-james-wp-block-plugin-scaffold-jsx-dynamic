import React from 'react';

const BlockComponent = props => {
  return (
    <div>
      {/* Your block's JSX code goes here */}
      <h1>Jess Custom Block</h1>
      {/* Example: Fetching data from WordPress REST API */}
      {/* ... */}
    </div>
  );
};

export default BlockComponent;
